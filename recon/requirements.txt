# Pip equivalents of github.com/eea/clms-hrwsi-api-client-python's env.yaml
# (that repo's own install instructions use conda; this project uses venv/pip
# instead per CLAUDE.md - all of these ship PyPI wheels so conda isn't needed).
boto3
geopandas
pyproj
pyogrio
shapely
retry
tqdm

# Added for inspecting downloaded GFSC rasters (not part of the client itself).
rasterio
numpy

# Added for recon/make_overlay.py, which writes the map overlay PNG.
pillow
